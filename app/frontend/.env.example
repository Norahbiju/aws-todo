# Leave empty in production so browser requests use the ALB's same origin.
NEXT_PUBLIC_API_BASE_URL=
# Used only by the Next.js server for local development or Docker Compose.
API_PROXY_TARGET=http://localhost:8000

